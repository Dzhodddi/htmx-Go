from pydantic import BaseModel, Field\nfrom langchain_core.tools import BaseTool\nimport os\nimport re\n\n\nclass CodebaseGrepToolInput(BaseModel):\n    search_directory: str = Field(description=\"Directory path to search for files\")\n    pattern: str = Field(description=\"Regex pattern to search for in files\")\n    file_extension: str = Field(default=None, description=\"Optional file extension filter (e.g., .py)\")\n    case_sensitive: bool = Field(default=False, description=\"Whether to perform case-sensitive search\")\n\n\nclass CodebaseGrepTool(BaseTool):\n    name: str = \"codebase_grep\"\n    description: str = \"Searches codebase directories for regex patterns with optional file extension filtering. Ignores binary files and common development directories.\"\n    args_schema: type[BaseModel] = CodebaseGrepToolInput\n\n    def _run(self, search_directory: str, pattern: str, file_extension: str = None, case_sensitive: bool = False) -> str:\n        try:\n            results = []\n            flags = 0 if case_sensitive else re.IGNORECASE\n            ignore_dirs = {\".git\", \"node_modules\", \"venv\", \".venv\", \"__pycache__\"}\n            \n            for root, dirs, files in os.walk(search_directory):\n                dirs[:] = [d for d in dirs if d not in ignore_dirs]\n                \n                for file in files:\n                    file_path = os.path.join(root, file)\n                    if file_extension and not file.endswith(file_extension):\n                        continue\n                    \n                    try:\n                        with open(file_path, 'r', encoding='utf-8') as f:\n                            for line_num, line in enumerate(f, 1):\n                                if re.search(pattern, line, flags):\n                                    results.append(f\"{file_path}:{line_num}: {line.strip()}\")\n                                    if len(results) >= 100:\n                                        return \"Search results capped at 100 matches:\n\" + \"\n\".join(results)\n                    except UnicodeDecodeError:\n                        continue\n            \n            return \"Search results:\n\" + \"\n\".join(results) if results else \"No matches found\"\n            \n        except Exception as e:\n            return f\"Error executing codebase_grep: {str(e)}\"\n